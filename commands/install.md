---
description: Install and launch the last built app on simulator/device
---

Load and follow the `smart-build` skill, but skip the build step. Only execute the install and launch steps for the most recently built app.
